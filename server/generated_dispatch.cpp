/* AUTO-GENERATED by codegen/generate_stubs.py — DO NOT EDIT */

#include <cstring>
#include <cstdint>
#include <vector>
#include <unordered_map>
#include <dlfcn.h>
#include <cstdio>

/* Handle mapping: client handle (uint64_t) → server real handle (void*) */
static std::unordered_map<uint64_t, void*> g_handle_map;
static uint64_t g_next_handle = 0x1000;

static uint64_t register_handle(void *real) {
    uint64_t h = g_next_handle++;
    g_handle_map[h] = real;
    return h;
}

static void* lookup_handle(uint64_t h) {
    auto it = g_handle_map.find(h);
    return (it != g_handle_map.end()) ? it->second : nullptr;
}

static void remove_handle(uint64_t h) { g_handle_map.erase(h); }

/* Library loading */
static void *g_cublas_lib = nullptr;
static void *g_cudnn_lib = nullptr;
static void *g_cuda_rt_extra_lib = nullptr;

static void ensure_libs_loaded() {
    if (!g_cublas_lib) g_cublas_lib = dlopen("libcublas.so", RTLD_NOW | RTLD_GLOBAL);
    if (!g_cudnn_lib) g_cudnn_lib = dlopen("libcudnn.so", RTLD_NOW | RTLD_GLOBAL);
    if (!g_cuda_rt_extra_lib) g_cuda_rt_extra_lib = dlopen("libcudart.so", RTLD_NOW | RTLD_GLOBAL);
}

/* Dispatch function — called by server for GS_OP_LIB_CALL */
bool dispatch_lib_call(const uint8_t *data, uint32_t len,
                       std::vector<uint8_t> &response) {
    if (len < 3) return false;
    ensure_libs_loaded();
    uint8_t lib_id = data[0];
    uint16_t func_id; memcpy(&func_id, data + 1, 2);
    const uint8_t *args = data + 3;
    uint32_t args_len = len - 3;
    (void)args_len;

    int32_t ret = 0;
    response.clear();

    if (lib_id == 1 && func_id == 1) { /* cublasCreate_v2 */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasCreate_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *handle_real = nullptr;
        ret = (int32_t)fn(&handle_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(handle_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 2) { /* cublasDestroy_v2 */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDestroy_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        ret = (int32_t)fn(handle);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 3) { /* cublasSetStream_v2 */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSetStream_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        ret = (int32_t)fn(handle, stream);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 4) { /* cublasGetStream_v2 */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasGetStream_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t stream_buf[8] = {0};
        ret = (int32_t)fn(handle, stream_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), stream_buf, stream_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 5) { /* cublasSetMathMode */
      do {
        typedef int (*fn_t)(void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSetMathMode");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int mode; memcpy(&mode, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, mode);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 6) { /* cublasSetWorkspace_v2 */
      do {
        typedef int (*fn_t)(void*, void*, uint64_t);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSetWorkspace_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t workspace_h; memcpy(&workspace_h, args + off, 8); off += 8;
        void *workspace = (void*)(uintptr_t)workspace_h;
        uint64_t size; memcpy(&size, args + off, 8); off += 8;
        ret = (int32_t)fn(handle, workspace, size);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 7) { /* cublasSaxpy_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSaxpy_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, n, alpha_buf, x, incx, y, incy);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 8) { /* cublasDaxpy_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDaxpy_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, n, alpha_buf, x, incx, y, incy);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 9) { /* cublasSscal_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSscal_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, n, alpha_buf, x, incx);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 10) { /* cublasDscal_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDscal_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, n, alpha_buf, x, incx);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 11) { /* cublasScopy_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasScopy_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, n, x, incx, y, incy);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 12) { /* cublasDcopy_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDcopy_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, n, x, incx, y, incy);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 13) { /* cublasSdot_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, int, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSdot_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        uint8_t result_buf[4] = {0};
        ret = (int32_t)fn(handle, n, x, incx, y, incy, result_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), result_buf, result_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 14) { /* cublasDdot_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, int, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDdot_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        uint8_t result_buf[8] = {0};
        ret = (int32_t)fn(handle, n, x, incx, y, incy, result_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), result_buf, result_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 15) { /* cublasSnrm2_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSnrm2_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint8_t result_buf[4] = {0};
        ret = (int32_t)fn(handle, n, x, incx, result_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), result_buf, result_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 16) { /* cublasSasum_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSasum_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint8_t result_buf[4] = {0};
        ret = (int32_t)fn(handle, n, x, incx, result_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), result_buf, result_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 17) { /* cublasIsamax_v2 */
      do {
        typedef int (*fn_t)(void*, int, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasIsamax_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int n; memcpy(&n, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint8_t result_buf[4] = {0};
        ret = (int32_t)fn(handle, n, x, incx, result_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), result_buf, result_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 18) { /* cublasSgemv_v2 */
      do {
        typedef int (*fn_t)(void*, int, int, int, void*, void*, int, void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSgemv_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int trans; memcpy(&trans, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint8_t beta_buf[4]; memcpy(beta_buf, args + off, 4); off += 4;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, trans, m, n, alpha_buf, A, lda, x, incx, beta_buf, y, incy);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 19) { /* cublasDgemv_v2 */
      do {
        typedef int (*fn_t)(void*, int, int, int, void*, void*, int, void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDgemv_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int trans; memcpy(&trans, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        int incx; memcpy(&incx, args + off, 4); off += 4;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        int incy; memcpy(&incy, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, trans, m, n, alpha_buf, A, lda, x, incx, beta_buf, y, incy);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 20) { /* cublasSgemm_v2 */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, void*, void*, int, void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSgemm_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        uint8_t beta_buf[4]; memcpy(beta_buf, args + off, 4); off += 4;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, k, alpha_buf, A, lda, B, ldb, beta_buf, C, ldc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 21) { /* cublasDgemm_v2 */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, void*, void*, int, void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDgemm_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, k, alpha_buf, A, lda, B, ldb, beta_buf, C, ldc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 22) { /* cublasHgemm */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, void*, void*, int, void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasHgemm");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        uint8_t alpha_buf[2]; memcpy(alpha_buf, args + off, 2); off += 2;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        uint8_t beta_buf[2]; memcpy(beta_buf, args + off, 2); off += 2;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, k, alpha_buf, A, lda, B, ldb, beta_buf, C, ldc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 23) { /* cublasGemmEx */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, void*, void*, int, int, void*, int, int, void*, void*, int, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasGemmEx");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int Atype; memcpy(&Atype, args + off, 4); off += 4;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int Btype; memcpy(&Btype, args + off, 4); off += 4;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int Ctype; memcpy(&Ctype, args + off, 4); off += 4;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        int computeType; memcpy(&computeType, args + off, 4); off += 4;
        int algo; memcpy(&algo, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, k, alpha_buf, A, Atype, lda, B, Btype, ldb, beta_buf, C, Ctype, ldc, computeType, algo);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 24) { /* cublasSgemmStridedBatched */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, void*, void*, int, int64_t, void*, int, int64_t, void*, void*, int, int64_t, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSgemmStridedBatched");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        int64_t strideA; memcpy(&strideA, args + off, 8); off += 8;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        int64_t strideB; memcpy(&strideB, args + off, 8); off += 8;
        uint8_t beta_buf[4]; memcpy(beta_buf, args + off, 4); off += 4;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        int64_t strideC; memcpy(&strideC, args + off, 8); off += 8;
        int batchCount; memcpy(&batchCount, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, k, alpha_buf, A, lda, strideA, B, ldb, strideB, beta_buf, C, ldc, strideC, batchCount);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 25) { /* cublasGemmStridedBatchedEx */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, void*, void*, int, int, int64_t, void*, int, int, int64_t, void*, void*, int, int, int64_t, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasGemmStridedBatchedEx");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int Atype; memcpy(&Atype, args + off, 4); off += 4;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        int64_t strideA; memcpy(&strideA, args + off, 8); off += 8;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int Btype; memcpy(&Btype, args + off, 4); off += 4;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        int64_t strideB; memcpy(&strideB, args + off, 8); off += 8;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int Ctype; memcpy(&Ctype, args + off, 4); off += 4;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        int64_t strideC; memcpy(&strideC, args + off, 8); off += 8;
        int batchCount; memcpy(&batchCount, args + off, 4); off += 4;
        int computeType; memcpy(&computeType, args + off, 4); off += 4;
        int algo; memcpy(&algo, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, k, alpha_buf, A, Atype, lda, strideA, B, Btype, ldb, strideB, beta_buf, C, Ctype, ldc, strideC, batchCount, computeType, algo);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 26) { /* cublasStrsm_v2 */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, int, void*, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasStrsm_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int side; memcpy(&side, args + off, 4); off += 4;
        int uplo; memcpy(&uplo, args + off, 4); off += 4;
        int trans; memcpy(&trans, args + off, 4); off += 4;
        int diag; memcpy(&diag, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, side, uplo, trans, diag, m, n, alpha_buf, A, lda, B, ldb);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 27) { /* cublasDtrsm_v2 */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, int, void*, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDtrsm_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int side; memcpy(&side, args + off, 4); off += 4;
        int uplo; memcpy(&uplo, args + off, 4); off += 4;
        int trans; memcpy(&trans, args + off, 4); off += 4;
        int diag; memcpy(&diag, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, side, uplo, trans, diag, m, n, alpha_buf, A, lda, B, ldb);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 28) { /* cublasSsyrk_v2 */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, void*, void*, int, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSsyrk_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int uplo; memcpy(&uplo, args + off, 4); off += 4;
        int trans; memcpy(&trans, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint8_t beta_buf[4]; memcpy(beta_buf, args + off, 4); off += 4;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, uplo, trans, n, k, alpha_buf, A, lda, beta_buf, C, ldc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 29) { /* cublasSgeam */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, void*, void*, int, void*, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSgeam");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[4]; memcpy(alpha_buf, args + off, 4); off += 4;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint8_t beta_buf[4]; memcpy(beta_buf, args + off, 4); off += 4;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, alpha_buf, A, lda, beta_buf, B, ldb, C, ldc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 30) { /* cublasDgeam */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, void*, void*, int, void*, void*, int, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasDgeam");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int transa; memcpy(&transa, args + off, 4); off += 4;
        int transb; memcpy(&transb, args + off, 4); off += 4;
        int m; memcpy(&m, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        int lda; memcpy(&lda, args + off, 4); off += 4;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        int ldb; memcpy(&ldb, args + off, 4); off += 4;
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        int ldc; memcpy(&ldc, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, transa, transb, m, n, alpha_buf, A, lda, beta_buf, B, ldb, C, ldc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 31) { /* cublasGetVersion_v2 */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasGetVersion_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t version_buf[4] = {0};
        ret = (int32_t)fn(handle, version_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), version_buf, version_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 32) { /* cublasSetPointerMode_v2 */
      do {
        typedef int (*fn_t)(void*, int);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasSetPointerMode_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int mode; memcpy(&mode, args + off, 4); off += 4;
        ret = (int32_t)fn(handle, mode);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 1 && func_id == 33) { /* cublasGetPointerMode_v2 */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cublas_lib, "cublasGetPointerMode_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t mode_buf[4] = {0};
        ret = (int32_t)fn(handle, mode_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), mode_buf, mode_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 34) { /* cudnnCreate */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreate");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *handle_real = nullptr;
        ret = (int32_t)fn(&handle_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(handle_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 35) { /* cudnnDestroy */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroy");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        ret = (int32_t)fn(handle);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 36) { /* cudnnSetStream */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetStream");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        ret = (int32_t)fn(handle, stream);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 37) { /* cudnnGetStream */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetStream");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t stream_buf[8] = {0};
        ret = (int32_t)fn(handle, stream_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), stream_buf, stream_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 38) { /* cudnnCreateTensorDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 39) { /* cudnnDestroyTensorDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 40) { /* cudnnSetTensor4dDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetTensor4dDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int format; memcpy(&format, args + off, 4); off += 4;
        int dataType; memcpy(&dataType, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int c; memcpy(&c, args + off, 4); off += 4;
        int h; memcpy(&h, args + off, 4); off += 4;
        int w; memcpy(&w, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, format, dataType, n, c, h, w);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 41) { /* cudnnSetTensor4dDescriptorEx */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, int, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetTensor4dDescriptorEx");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int dataType; memcpy(&dataType, args + off, 4); off += 4;
        int n; memcpy(&n, args + off, 4); off += 4;
        int c; memcpy(&c, args + off, 4); off += 4;
        int h; memcpy(&h, args + off, 4); off += 4;
        int w; memcpy(&w, args + off, 4); off += 4;
        int nStride; memcpy(&nStride, args + off, 4); off += 4;
        int cStride; memcpy(&cStride, args + off, 4); off += 4;
        int hStride; memcpy(&hStride, args + off, 4); off += 4;
        int wStride; memcpy(&wStride, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, dataType, n, c, h, w, nStride, cStride, hStride, wStride);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 42) { /* cudnnCreateFilterDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateFilterDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 43) { /* cudnnDestroyFilterDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyFilterDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 44) { /* cudnnSetFilter4dDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetFilter4dDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int dataType; memcpy(&dataType, args + off, 4); off += 4;
        int format; memcpy(&format, args + off, 4); off += 4;
        int k; memcpy(&k, args + off, 4); off += 4;
        int c; memcpy(&c, args + off, 4); off += 4;
        int h; memcpy(&h, args + off, 4); off += 4;
        int w; memcpy(&w, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, dataType, format, k, c, h, w);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 45) { /* cudnnCreateConvolutionDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateConvolutionDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 46) { /* cudnnDestroyConvolutionDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyConvolutionDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 47) { /* cudnnSetConvolution2dDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetConvolution2dDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int padH; memcpy(&padH, args + off, 4); off += 4;
        int padW; memcpy(&padW, args + off, 4); off += 4;
        int strideH; memcpy(&strideH, args + off, 4); off += 4;
        int strideW; memcpy(&strideW, args + off, 4); off += 4;
        int dilationH; memcpy(&dilationH, args + off, 4); off += 4;
        int dilationW; memcpy(&dilationW, args + off, 4); off += 4;
        int mode; memcpy(&mode, args + off, 4); off += 4;
        int computeType; memcpy(&computeType, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, padH, padW, strideH, strideW, dilationH, dilationW, mode, computeType);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 48) { /* cudnnSetConvolutionGroupCount */
      do {
        typedef int (*fn_t)(void*, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetConvolutionGroupCount");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int groupCount; memcpy(&groupCount, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, groupCount);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 49) { /* cudnnSetConvolutionMathType */
      do {
        typedef int (*fn_t)(void*, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetConvolutionMathType");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int mathType; memcpy(&mathType, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, mathType);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 50) { /* cudnnGetConvolution2dForwardOutputDim */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetConvolution2dForwardOutputDim");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        uint64_t inputDesc_h; memcpy(&inputDesc_h, args + off, 8); off += 8;
        void *inputDesc = lookup_handle(inputDesc_h);
        uint64_t filterDesc_h; memcpy(&filterDesc_h, args + off, 8); off += 8;
        void *filterDesc = lookup_handle(filterDesc_h);
        uint8_t n_buf[4] = {0};
        uint8_t c_buf[4] = {0};
        uint8_t h_buf[4] = {0};
        uint8_t w_buf[4] = {0};
        ret = (int32_t)fn(desc, inputDesc, filterDesc, n_buf, c_buf, h_buf, w_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), n_buf, n_buf + 4);
        response.insert(response.end(), c_buf, c_buf + 4);
        response.insert(response.end(), h_buf, h_buf + 4);
        response.insert(response.end(), w_buf, w_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 51) { /* cudnnGetConvolutionForwardWorkspaceSize */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetConvolutionForwardWorkspaceSize");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t wDesc_h; memcpy(&wDesc_h, args + off, 8); off += 8;
        void *wDesc = lookup_handle(wDesc_h);
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        uint8_t sizeInBytes_buf[8] = {0};
        ret = (int32_t)fn(handle, xDesc, wDesc, convDesc, yDesc, algo, sizeInBytes_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), sizeInBytes_buf, sizeInBytes_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 52) { /* cudnnConvolutionForward */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, int, void*, uint64_t, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnConvolutionForward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint64_t wDesc_h; memcpy(&wDesc_h, args + off, 8); off += 8;
        void *wDesc = lookup_handle(wDesc_h);
        uint64_t w_h; memcpy(&w_h, args + off, 8); off += 8;
        void *w = (void*)(uintptr_t)w_h;
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        uint64_t workspace_h; memcpy(&workspace_h, args + off, 8); off += 8;
        void *workspace = (void*)(uintptr_t)workspace_h;
        uint64_t workspaceSizeInBytes; memcpy(&workspaceSizeInBytes, args + off, 8); off += 8;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        ret = (int32_t)fn(handle, alpha_buf, xDesc, x, wDesc, w, convDesc, algo, workspace, workspaceSizeInBytes, beta_buf, yDesc, y);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 53) { /* cudnnConvolutionBackwardData */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, int, void*, uint64_t, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnConvolutionBackwardData");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t wDesc_h; memcpy(&wDesc_h, args + off, 8); off += 8;
        void *wDesc = lookup_handle(wDesc_h);
        uint64_t w_h; memcpy(&w_h, args + off, 8); off += 8;
        void *w = (void*)(uintptr_t)w_h;
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t dy_h; memcpy(&dy_h, args + off, 8); off += 8;
        void *dy = (void*)(uintptr_t)dy_h;
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        uint64_t workspace_h; memcpy(&workspace_h, args + off, 8); off += 8;
        void *workspace = (void*)(uintptr_t)workspace_h;
        uint64_t workspaceSizeInBytes; memcpy(&workspaceSizeInBytes, args + off, 8); off += 8;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t dxDesc_h; memcpy(&dxDesc_h, args + off, 8); off += 8;
        void *dxDesc = lookup_handle(dxDesc_h);
        uint64_t dx_h; memcpy(&dx_h, args + off, 8); off += 8;
        void *dx = (void*)(uintptr_t)dx_h;
        ret = (int32_t)fn(handle, alpha_buf, wDesc, w, dyDesc, dy, convDesc, algo, workspace, workspaceSizeInBytes, beta_buf, dxDesc, dx);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 54) { /* cudnnConvolutionBackwardFilter */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, int, void*, uint64_t, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnConvolutionBackwardFilter");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t dy_h; memcpy(&dy_h, args + off, 8); off += 8;
        void *dy = (void*)(uintptr_t)dy_h;
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        uint64_t workspace_h; memcpy(&workspace_h, args + off, 8); off += 8;
        void *workspace = (void*)(uintptr_t)workspace_h;
        uint64_t workspaceSizeInBytes; memcpy(&workspaceSizeInBytes, args + off, 8); off += 8;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t dwDesc_h; memcpy(&dwDesc_h, args + off, 8); off += 8;
        void *dwDesc = lookup_handle(dwDesc_h);
        uint64_t dw_h; memcpy(&dw_h, args + off, 8); off += 8;
        void *dw = (void*)(uintptr_t)dw_h;
        ret = (int32_t)fn(handle, alpha_buf, xDesc, x, dyDesc, dy, convDesc, algo, workspace, workspaceSizeInBytes, beta_buf, dwDesc, dw);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 55) { /* cudnnCreateActivationDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateActivationDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 56) { /* cudnnDestroyActivationDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyActivationDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 57) { /* cudnnSetActivationDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, double);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetActivationDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int mode; memcpy(&mode, args + off, 4); off += 4;
        int reluNanOpt; memcpy(&reluNanOpt, args + off, 4); off += 4;
        double coef; memcpy(&coef, args + off, 8); off += 8;
        ret = (int32_t)fn(desc, mode, reluNanOpt, coef);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 58) { /* cudnnActivationForward */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnActivationForward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t activationDesc_h; memcpy(&activationDesc_h, args + off, 8); off += 8;
        void *activationDesc = lookup_handle(activationDesc_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        ret = (int32_t)fn(handle, activationDesc, alpha_buf, xDesc, x, beta_buf, yDesc, y);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 59) { /* cudnnActivationBackward */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnActivationBackward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t activationDesc_h; memcpy(&activationDesc_h, args + off, 8); off += 8;
        void *activationDesc = lookup_handle(activationDesc_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t dy_h; memcpy(&dy_h, args + off, 8); off += 8;
        void *dy = (void*)(uintptr_t)dy_h;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t dxDesc_h; memcpy(&dxDesc_h, args + off, 8); off += 8;
        void *dxDesc = lookup_handle(dxDesc_h);
        uint64_t dx_h; memcpy(&dx_h, args + off, 8); off += 8;
        void *dx = (void*)(uintptr_t)dx_h;
        ret = (int32_t)fn(handle, activationDesc, alpha_buf, yDesc, y, dyDesc, dy, xDesc, x, beta_buf, dxDesc, dx);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 60) { /* cudnnCreatePoolingDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreatePoolingDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 61) { /* cudnnDestroyPoolingDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyPoolingDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 62) { /* cudnnSetPooling2dDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetPooling2dDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int mode; memcpy(&mode, args + off, 4); off += 4;
        int maxpoolingNanOpt; memcpy(&maxpoolingNanOpt, args + off, 4); off += 4;
        int windowH; memcpy(&windowH, args + off, 4); off += 4;
        int windowW; memcpy(&windowW, args + off, 4); off += 4;
        int padH; memcpy(&padH, args + off, 4); off += 4;
        int padW; memcpy(&padW, args + off, 4); off += 4;
        int strideH; memcpy(&strideH, args + off, 4); off += 4;
        int strideW; memcpy(&strideW, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, mode, maxpoolingNanOpt, windowH, windowW, padH, padW, strideH, strideW);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 63) { /* cudnnPoolingForward */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnPoolingForward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t poolingDesc_h; memcpy(&poolingDesc_h, args + off, 8); off += 8;
        void *poolingDesc = lookup_handle(poolingDesc_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        ret = (int32_t)fn(handle, poolingDesc, alpha_buf, xDesc, x, beta_buf, yDesc, y);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 64) { /* cudnnSoftmaxForward */
      do {
        typedef int (*fn_t)(void*, int, int, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSoftmaxForward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        int mode; memcpy(&mode, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        ret = (int32_t)fn(handle, algo, mode, alpha_buf, xDesc, x, beta_buf, yDesc, y);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 65) { /* cudnnSoftmaxBackward */
      do {
        typedef int (*fn_t)(void*, int, int, void*, void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSoftmaxBackward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        int mode; memcpy(&mode, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t dy_h; memcpy(&dy_h, args + off, 8); off += 8;
        void *dy = (void*)(uintptr_t)dy_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t dxDesc_h; memcpy(&dxDesc_h, args + off, 8); off += 8;
        void *dxDesc = lookup_handle(dxDesc_h);
        uint64_t dx_h; memcpy(&dx_h, args + off, 8); off += 8;
        void *dx = (void*)(uintptr_t)dx_h;
        ret = (int32_t)fn(handle, algo, mode, alpha_buf, yDesc, y, dyDesc, dy, beta_buf, dxDesc, dx);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 66) { /* cudnnBatchNormalizationForwardTraining */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, void*, void*, void*, void*, void*, void*, void*, double, void*, void*, double, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnBatchNormalizationForwardTraining");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int mode; memcpy(&mode, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        uint64_t bnScaleBiasMeanVarDesc_h; memcpy(&bnScaleBiasMeanVarDesc_h, args + off, 8); off += 8;
        void *bnScaleBiasMeanVarDesc = lookup_handle(bnScaleBiasMeanVarDesc_h);
        uint64_t bnScale_h; memcpy(&bnScale_h, args + off, 8); off += 8;
        void *bnScale = (void*)(uintptr_t)bnScale_h;
        uint64_t bnBias_h; memcpy(&bnBias_h, args + off, 8); off += 8;
        void *bnBias = (void*)(uintptr_t)bnBias_h;
        double expAvgFactor; memcpy(&expAvgFactor, args + off, 8); off += 8;
        uint64_t resultRunningMean_h; memcpy(&resultRunningMean_h, args + off, 8); off += 8;
        void *resultRunningMean = (void*)(uintptr_t)resultRunningMean_h;
        uint64_t resultRunningVariance_h; memcpy(&resultRunningVariance_h, args + off, 8); off += 8;
        void *resultRunningVariance = (void*)(uintptr_t)resultRunningVariance_h;
        double epsilon; memcpy(&epsilon, args + off, 8); off += 8;
        uint64_t resultSaveMean_h; memcpy(&resultSaveMean_h, args + off, 8); off += 8;
        void *resultSaveMean = (void*)(uintptr_t)resultSaveMean_h;
        uint64_t resultSaveInvVariance_h; memcpy(&resultSaveInvVariance_h, args + off, 8); off += 8;
        void *resultSaveInvVariance = (void*)(uintptr_t)resultSaveInvVariance_h;
        ret = (int32_t)fn(handle, mode, alpha_buf, beta_buf, xDesc, x, yDesc, y, bnScaleBiasMeanVarDesc, bnScale, bnBias, expAvgFactor, resultRunningMean, resultRunningVariance, epsilon, resultSaveMean, resultSaveInvVariance);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 67) { /* cudnnBatchNormalizationForwardInference */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, double);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnBatchNormalizationForwardInference");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int mode; memcpy(&mode, args + off, 4); off += 4;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        uint64_t bnScaleBiasMeanVarDesc_h; memcpy(&bnScaleBiasMeanVarDesc_h, args + off, 8); off += 8;
        void *bnScaleBiasMeanVarDesc = lookup_handle(bnScaleBiasMeanVarDesc_h);
        uint64_t bnScale_h; memcpy(&bnScale_h, args + off, 8); off += 8;
        void *bnScale = (void*)(uintptr_t)bnScale_h;
        uint64_t bnBias_h; memcpy(&bnBias_h, args + off, 8); off += 8;
        void *bnBias = (void*)(uintptr_t)bnBias_h;
        uint64_t estimatedMean_h; memcpy(&estimatedMean_h, args + off, 8); off += 8;
        void *estimatedMean = (void*)(uintptr_t)estimatedMean_h;
        uint64_t estimatedVariance_h; memcpy(&estimatedVariance_h, args + off, 8); off += 8;
        void *estimatedVariance = (void*)(uintptr_t)estimatedVariance_h;
        double epsilon; memcpy(&epsilon, args + off, 8); off += 8;
        ret = (int32_t)fn(handle, mode, alpha_buf, beta_buf, xDesc, x, yDesc, y, bnScaleBiasMeanVarDesc, bnScale, bnBias, estimatedMean, estimatedVariance, epsilon);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 68) { /* cudnnBatchNormalizationBackward */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, double, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnBatchNormalizationBackward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        int mode; memcpy(&mode, args + off, 4); off += 4;
        uint8_t alphaDataDiff_buf[8]; memcpy(alphaDataDiff_buf, args + off, 8); off += 8;
        uint8_t betaDataDiff_buf[8]; memcpy(betaDataDiff_buf, args + off, 8); off += 8;
        uint8_t alphaParamDiff_buf[8]; memcpy(alphaParamDiff_buf, args + off, 8); off += 8;
        uint8_t betaParamDiff_buf[8]; memcpy(betaParamDiff_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t dy_h; memcpy(&dy_h, args + off, 8); off += 8;
        void *dy = (void*)(uintptr_t)dy_h;
        uint64_t dxDesc_h; memcpy(&dxDesc_h, args + off, 8); off += 8;
        void *dxDesc = lookup_handle(dxDesc_h);
        uint64_t dx_h; memcpy(&dx_h, args + off, 8); off += 8;
        void *dx = (void*)(uintptr_t)dx_h;
        uint64_t bnScaleBiasDiffDesc_h; memcpy(&bnScaleBiasDiffDesc_h, args + off, 8); off += 8;
        void *bnScaleBiasDiffDesc = lookup_handle(bnScaleBiasDiffDesc_h);
        uint64_t bnScale_h; memcpy(&bnScale_h, args + off, 8); off += 8;
        void *bnScale = (void*)(uintptr_t)bnScale_h;
        uint64_t resultBnScaleDiff_h; memcpy(&resultBnScaleDiff_h, args + off, 8); off += 8;
        void *resultBnScaleDiff = (void*)(uintptr_t)resultBnScaleDiff_h;
        uint64_t resultBnBiasDiff_h; memcpy(&resultBnBiasDiff_h, args + off, 8); off += 8;
        void *resultBnBiasDiff = (void*)(uintptr_t)resultBnBiasDiff_h;
        double epsilon; memcpy(&epsilon, args + off, 8); off += 8;
        uint64_t savedMean_h; memcpy(&savedMean_h, args + off, 8); off += 8;
        void *savedMean = (void*)(uintptr_t)savedMean_h;
        uint64_t savedInvVariance_h; memcpy(&savedInvVariance_h, args + off, 8); off += 8;
        void *savedInvVariance = (void*)(uintptr_t)savedInvVariance_h;
        ret = (int32_t)fn(handle, mode, alphaDataDiff_buf, betaDataDiff_buf, alphaParamDiff_buf, betaParamDiff_buf, xDesc, x, dyDesc, dy, dxDesc, dx, bnScaleBiasDiffDesc, bnScale, resultBnScaleDiff, resultBnBiasDiff, epsilon, savedMean, savedInvVariance);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 69) { /* cudnnAddTensor */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnAddTensor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t aDesc_h; memcpy(&aDesc_h, args + off, 8); off += 8;
        void *aDesc = lookup_handle(aDesc_h);
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t cDesc_h; memcpy(&cDesc_h, args + off, 8); off += 8;
        void *cDesc = lookup_handle(cDesc_h);
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        ret = (int32_t)fn(handle, alpha_buf, aDesc, A, beta_buf, cDesc, C);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 70) { /* cudnnScaleTensor */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnScaleTensor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        ret = (int32_t)fn(handle, yDesc, y, alpha_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 71) { /* cudnnTransformTensor */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnTransformTensor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        ret = (int32_t)fn(handle, alpha_buf, xDesc, x, beta_buf, yDesc, y);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 72) { /* cudnnGetVersion */
      do {
        typedef uint64_t (*fn_t)(void);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetVersion");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        ret = (int32_t)fn();
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 73) { /* cudnnCreateDropoutDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateDropoutDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 74) { /* cudnnDestroyDropoutDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyDropoutDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 75) { /* cudnnDropoutGetStatesSize */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDropoutGetStatesSize");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint8_t sizeInBytes_buf[8] = {0};
        ret = (int32_t)fn(handle, sizeInBytes_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), sizeInBytes_buf, sizeInBytes_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 76) { /* cudnnCreateRNNDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateRNNDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 77) { /* cudnnDestroyRNNDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyRNNDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 78) { /* cudnnCreateOpTensorDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateOpTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 79) { /* cudnnDestroyOpTensorDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyOpTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 80) { /* cudnnSetOpTensorDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetOpTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int opTensorOp; memcpy(&opTensorOp, args + off, 4); off += 4;
        int opTensorCompType; memcpy(&opTensorCompType, args + off, 4); off += 4;
        int opTensorNanOpt; memcpy(&opTensorNanOpt, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, opTensorOp, opTensorCompType, opTensorNanOpt);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 81) { /* cudnnOpTensor */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnOpTensor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t opTensorDesc_h; memcpy(&opTensorDesc_h, args + off, 8); off += 8;
        void *opTensorDesc = lookup_handle(opTensorDesc_h);
        uint8_t alpha1_buf[8]; memcpy(alpha1_buf, args + off, 8); off += 8;
        uint64_t aDesc_h; memcpy(&aDesc_h, args + off, 8); off += 8;
        void *aDesc = lookup_handle(aDesc_h);
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        uint8_t alpha2_buf[8]; memcpy(alpha2_buf, args + off, 8); off += 8;
        uint64_t bDesc_h; memcpy(&bDesc_h, args + off, 8); off += 8;
        void *bDesc = lookup_handle(bDesc_h);
        uint64_t B_h; memcpy(&B_h, args + off, 8); off += 8;
        void *B = (void*)(uintptr_t)B_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t cDesc_h; memcpy(&cDesc_h, args + off, 8); off += 8;
        void *cDesc = lookup_handle(cDesc_h);
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        ret = (int32_t)fn(handle, opTensorDesc, alpha1_buf, aDesc, A, alpha2_buf, bDesc, B, beta_buf, cDesc, C);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 82) { /* cudnnCreateReduceTensorDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateReduceTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 83) { /* cudnnDestroyReduceTensorDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyReduceTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 84) { /* cudnnSetReduceTensorDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, int, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetReduceTensorDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int op; memcpy(&op, args + off, 4); off += 4;
        int compType; memcpy(&compType, args + off, 4); off += 4;
        int nanOpt; memcpy(&nanOpt, args + off, 4); off += 4;
        int indices; memcpy(&indices, args + off, 4); off += 4;
        int indicesType; memcpy(&indicesType, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, op, compType, nanOpt, indices, indicesType);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 85) { /* cudnnGetReductionWorkspaceSize */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetReductionWorkspaceSize");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t reduceDesc_h; memcpy(&reduceDesc_h, args + off, 8); off += 8;
        void *reduceDesc = lookup_handle(reduceDesc_h);
        uint64_t aDesc_h; memcpy(&aDesc_h, args + off, 8); off += 8;
        void *aDesc = lookup_handle(aDesc_h);
        uint64_t cDesc_h; memcpy(&cDesc_h, args + off, 8); off += 8;
        void *cDesc = lookup_handle(cDesc_h);
        uint8_t sizeInBytes_buf[8] = {0};
        ret = (int32_t)fn(handle, reduceDesc, aDesc, cDesc, sizeInBytes_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), sizeInBytes_buf, sizeInBytes_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 86) { /* cudnnReduceTensor */
      do {
        typedef int (*fn_t)(void*, void*, void*, uint64_t, void*, uint64_t, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnReduceTensor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t reduceDesc_h; memcpy(&reduceDesc_h, args + off, 8); off += 8;
        void *reduceDesc = lookup_handle(reduceDesc_h);
        uint64_t indices_h; memcpy(&indices_h, args + off, 8); off += 8;
        void *indices = (void*)(uintptr_t)indices_h;
        uint64_t indicesSizeInBytes; memcpy(&indicesSizeInBytes, args + off, 8); off += 8;
        uint64_t workspace_h; memcpy(&workspace_h, args + off, 8); off += 8;
        void *workspace = (void*)(uintptr_t)workspace_h;
        uint64_t workspaceSizeInBytes; memcpy(&workspaceSizeInBytes, args + off, 8); off += 8;
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t aDesc_h; memcpy(&aDesc_h, args + off, 8); off += 8;
        void *aDesc = lookup_handle(aDesc_h);
        uint64_t A_h; memcpy(&A_h, args + off, 8); off += 8;
        void *A = (void*)(uintptr_t)A_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t cDesc_h; memcpy(&cDesc_h, args + off, 8); off += 8;
        void *cDesc = lookup_handle(cDesc_h);
        uint64_t C_h; memcpy(&C_h, args + off, 8); off += 8;
        void *C = (void*)(uintptr_t)C_h;
        ret = (int32_t)fn(handle, reduceDesc, indices, indicesSizeInBytes, workspace, workspaceSizeInBytes, alpha_buf, aDesc, A, beta_buf, cDesc, C);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 87) { /* cudnnSetTensorNdDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetTensorNdDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int dataType; memcpy(&dataType, args + off, 4); off += 4;
        int nbDims; memcpy(&nbDims, args + off, 4); off += 4;
        uint8_t dimA_buf[32]; memcpy(dimA_buf, args + off, 32); off += 32;
        uint8_t strideA_buf[32]; memcpy(strideA_buf, args + off, 32); off += 32;
        ret = (int32_t)fn(desc, dataType, nbDims, dimA_buf, strideA_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 88) { /* cudnnSetFilterNdDescriptor */
      do {
        typedef int (*fn_t)(void*, int, int, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetFilterNdDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int dataType; memcpy(&dataType, args + off, 4); off += 4;
        int format; memcpy(&format, args + off, 4); off += 4;
        int nbDims; memcpy(&nbDims, args + off, 4); off += 4;
        uint8_t filterDimA_buf[32]; memcpy(filterDimA_buf, args + off, 32); off += 32;
        ret = (int32_t)fn(desc, dataType, format, nbDims, filterDimA_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 89) { /* cudnnSetConvolutionNdDescriptor */
      do {
        typedef int (*fn_t)(void*, int, void*, void*, void*, int, int);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetConvolutionNdDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int arrayLength; memcpy(&arrayLength, args + off, 4); off += 4;
        uint8_t padA_buf[24]; memcpy(padA_buf, args + off, 24); off += 24;
        uint8_t filterStrideA_buf[24]; memcpy(filterStrideA_buf, args + off, 24); off += 24;
        uint8_t dilationA_buf[24]; memcpy(dilationA_buf, args + off, 24); off += 24;
        int mode; memcpy(&mode, args + off, 4); off += 4;
        int computeType; memcpy(&computeType, args + off, 4); off += 4;
        ret = (int32_t)fn(desc, arrayLength, padA_buf, filterStrideA_buf, dilationA_buf, mode, computeType);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 90) { /* cudnnGetConvolutionForwardAlgorithm_v7 */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, int, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetConvolutionForwardAlgorithm_v7");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t srcDesc_h; memcpy(&srcDesc_h, args + off, 8); off += 8;
        void *srcDesc = lookup_handle(srcDesc_h);
        uint64_t filterDesc_h; memcpy(&filterDesc_h, args + off, 8); off += 8;
        void *filterDesc = lookup_handle(filterDesc_h);
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        uint64_t destDesc_h; memcpy(&destDesc_h, args + off, 8); off += 8;
        void *destDesc = lookup_handle(destDesc_h);
        int requestedAlgoCount; memcpy(&requestedAlgoCount, args + off, 4); off += 4;
        uint8_t returnedAlgoCount_buf[4] = {0};
        uint8_t perfResults_buf[512] = {0};
        ret = (int32_t)fn(handle, srcDesc, filterDesc, convDesc, destDesc, requestedAlgoCount, returnedAlgoCount_buf, perfResults_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), returnedAlgoCount_buf, returnedAlgoCount_buf + 4);
        response.insert(response.end(), perfResults_buf, perfResults_buf + 512);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 91) { /* cudnnGetConvolutionBackwardDataAlgorithm_v7 */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, int, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetConvolutionBackwardDataAlgorithm_v7");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t filterDesc_h; memcpy(&filterDesc_h, args + off, 8); off += 8;
        void *filterDesc = lookup_handle(filterDesc_h);
        uint64_t diffDesc_h; memcpy(&diffDesc_h, args + off, 8); off += 8;
        void *diffDesc = lookup_handle(diffDesc_h);
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        uint64_t gradDesc_h; memcpy(&gradDesc_h, args + off, 8); off += 8;
        void *gradDesc = lookup_handle(gradDesc_h);
        int requestedAlgoCount; memcpy(&requestedAlgoCount, args + off, 4); off += 4;
        uint8_t returnedAlgoCount_buf[4] = {0};
        uint8_t perfResults_buf[512] = {0};
        ret = (int32_t)fn(handle, filterDesc, diffDesc, convDesc, gradDesc, requestedAlgoCount, returnedAlgoCount_buf, perfResults_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), returnedAlgoCount_buf, returnedAlgoCount_buf + 4);
        response.insert(response.end(), perfResults_buf, perfResults_buf + 512);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 92) { /* cudnnGetConvolutionBackwardFilterAlgorithm_v7 */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, int, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetConvolutionBackwardFilterAlgorithm_v7");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t srcDesc_h; memcpy(&srcDesc_h, args + off, 8); off += 8;
        void *srcDesc = lookup_handle(srcDesc_h);
        uint64_t diffDesc_h; memcpy(&diffDesc_h, args + off, 8); off += 8;
        void *diffDesc = lookup_handle(diffDesc_h);
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        uint64_t gradDesc_h; memcpy(&gradDesc_h, args + off, 8); off += 8;
        void *gradDesc = lookup_handle(gradDesc_h);
        int requestedAlgoCount; memcpy(&requestedAlgoCount, args + off, 4); off += 4;
        uint8_t returnedAlgoCount_buf[4] = {0};
        uint8_t perfResults_buf[512] = {0};
        ret = (int32_t)fn(handle, srcDesc, diffDesc, convDesc, gradDesc, requestedAlgoCount, returnedAlgoCount_buf, perfResults_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), returnedAlgoCount_buf, returnedAlgoCount_buf + 4);
        response.insert(response.end(), perfResults_buf, perfResults_buf + 512);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 93) { /* cudnnGetConvolutionBackwardDataWorkspaceSize */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetConvolutionBackwardDataWorkspaceSize");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t wDesc_h; memcpy(&wDesc_h, args + off, 8); off += 8;
        void *wDesc = lookup_handle(wDesc_h);
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        uint64_t dxDesc_h; memcpy(&dxDesc_h, args + off, 8); off += 8;
        void *dxDesc = lookup_handle(dxDesc_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        uint8_t sizeInBytes_buf[8] = {0};
        ret = (int32_t)fn(handle, wDesc, dyDesc, convDesc, dxDesc, algo, sizeInBytes_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), sizeInBytes_buf, sizeInBytes_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 94) { /* cudnnGetConvolutionBackwardFilterWorkspaceSize */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, int, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnGetConvolutionBackwardFilterWorkspaceSize");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t convDesc_h; memcpy(&convDesc_h, args + off, 8); off += 8;
        void *convDesc = lookup_handle(convDesc_h);
        uint64_t gradDesc_h; memcpy(&gradDesc_h, args + off, 8); off += 8;
        void *gradDesc = lookup_handle(gradDesc_h);
        int algo; memcpy(&algo, args + off, 4); off += 4;
        uint8_t sizeInBytes_buf[8] = {0};
        ret = (int32_t)fn(handle, xDesc, dyDesc, convDesc, gradDesc, algo, sizeInBytes_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), sizeInBytes_buf, sizeInBytes_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 95) { /* cudnnSetTensor */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetTensor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        uint8_t valuePtr_buf[8]; memcpy(valuePtr_buf, args + off, 8); off += 8;
        ret = (int32_t)fn(handle, yDesc, y, valuePtr_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 96) { /* cudnnPoolingBackward */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnPoolingBackward");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t handle_h; memcpy(&handle_h, args + off, 8); off += 8;
        void *handle = lookup_handle(handle_h);
        uint64_t poolingDesc_h; memcpy(&poolingDesc_h, args + off, 8); off += 8;
        void *poolingDesc = lookup_handle(poolingDesc_h);
        uint8_t alpha_buf[8]; memcpy(alpha_buf, args + off, 8); off += 8;
        uint64_t yDesc_h; memcpy(&yDesc_h, args + off, 8); off += 8;
        void *yDesc = lookup_handle(yDesc_h);
        uint64_t y_h; memcpy(&y_h, args + off, 8); off += 8;
        void *y = (void*)(uintptr_t)y_h;
        uint64_t dyDesc_h; memcpy(&dyDesc_h, args + off, 8); off += 8;
        void *dyDesc = lookup_handle(dyDesc_h);
        uint64_t dy_h; memcpy(&dy_h, args + off, 8); off += 8;
        void *dy = (void*)(uintptr_t)dy_h;
        uint64_t xDesc_h; memcpy(&xDesc_h, args + off, 8); off += 8;
        void *xDesc = lookup_handle(xDesc_h);
        uint64_t x_h; memcpy(&x_h, args + off, 8); off += 8;
        void *x = (void*)(uintptr_t)x_h;
        uint8_t beta_buf[8]; memcpy(beta_buf, args + off, 8); off += 8;
        uint64_t dxDesc_h; memcpy(&dxDesc_h, args + off, 8); off += 8;
        void *dxDesc = lookup_handle(dxDesc_h);
        uint64_t dx_h; memcpy(&dx_h, args + off, 8); off += 8;
        void *dx = (void*)(uintptr_t)dx_h;
        ret = (int32_t)fn(handle, poolingDesc, alpha_buf, yDesc, y, dyDesc, dy, xDesc, x, beta_buf, dxDesc, dx);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 97) { /* cudnnCreateLRNDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnCreateLRNDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *desc_real = nullptr;
        ret = (int32_t)fn(&desc_real);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(desc_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 98) { /* cudnnDestroyLRNDescriptor */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnDestroyLRNDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        ret = (int32_t)fn(desc);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 2 && func_id == 99) { /* cudnnSetLRNDescriptor */
      do {
        typedef int (*fn_t)(void*, int, double, double, double);
        static fn_t fn = (fn_t)dlsym(g_cudnn_lib, "cudnnSetLRNDescriptor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t desc_h; memcpy(&desc_h, args + off, 8); off += 8;
        void *desc = lookup_handle(desc_h);
        int lrnN; memcpy(&lrnN, args + off, 4); off += 4;
        double lrnAlpha; memcpy(&lrnAlpha, args + off, 8); off += 8;
        double lrnBeta; memcpy(&lrnBeta, args + off, 8); off += 8;
        double lrnK; memcpy(&lrnK, args + off, 8); off += 8;
        ret = (int32_t)fn(desc, lrnN, lrnAlpha, lrnBeta, lrnK);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 100) { /* cudaMallocManaged */
      do {
        typedef int (*fn_t)(void*, uint64_t, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaMallocManaged");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *devPtr_real = nullptr;
        uint64_t size; memcpy(&size, args + off, 8); off += 8;
        int flags; memcpy(&flags, args + off, 4); off += 4;
        ret = (int32_t)fn(&devPtr_real, size, flags);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(devPtr_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 101) { /* cudaMallocHost */
      do {
        typedef int (*fn_t)(void*, uint64_t);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaMallocHost");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *ptr_real = nullptr;
        uint64_t size; memcpy(&size, args + off, 8); off += 8;
        ret = (int32_t)fn(&ptr_real, size);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(ptr_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 102) { /* cudaFreeHost */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaFreeHost");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t ptr_h; memcpy(&ptr_h, args + off, 8); off += 8;
        void *ptr = lookup_handle(ptr_h);
        ret = (int32_t)fn(ptr);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 103) { /* cudaMallocAsync */
      do {
        typedef int (*fn_t)(void*, uint64_t, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaMallocAsync");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *devPtr_real = nullptr;
        uint64_t size; memcpy(&size, args + off, 8); off += 8;
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        ret = (int32_t)fn(&devPtr_real, size, stream);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(devPtr_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 104) { /* cudaFreeAsync */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaFreeAsync");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t devPtr_h; memcpy(&devPtr_h, args + off, 8); off += 8;
        void *devPtr = lookup_handle(devPtr_h);
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        ret = (int32_t)fn(devPtr, stream);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 105) { /* cudaMemGetInfo */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaMemGetInfo");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t free_buf[8] = {0};
        uint8_t total_buf[8] = {0};
        ret = (int32_t)fn(free_buf, total_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), free_buf, free_buf + 8);
        response.insert(response.end(), total_buf, total_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 106) { /* cudaMemcpy2D */
      do {
        typedef int (*fn_t)(void*, uint64_t, void*, uint64_t, uint64_t, uint64_t, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaMemcpy2D");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t dst_h; memcpy(&dst_h, args + off, 8); off += 8;
        void *dst = (void*)(uintptr_t)dst_h;
        uint64_t dpitch; memcpy(&dpitch, args + off, 8); off += 8;
        uint64_t src_h; memcpy(&src_h, args + off, 8); off += 8;
        void *src = (void*)(uintptr_t)src_h;
        uint64_t spitch; memcpy(&spitch, args + off, 8); off += 8;
        uint64_t width; memcpy(&width, args + off, 8); off += 8;
        uint64_t height; memcpy(&height, args + off, 8); off += 8;
        int kind; memcpy(&kind, args + off, 4); off += 4;
        ret = (int32_t)fn(dst, dpitch, src, spitch, width, height, kind);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 107) { /* cudaMemsetAsync */
      do {
        typedef int (*fn_t)(void*, int, uint64_t, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaMemsetAsync");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t devPtr_h; memcpy(&devPtr_h, args + off, 8); off += 8;
        void *devPtr = (void*)(uintptr_t)devPtr_h;
        int value; memcpy(&value, args + off, 4); off += 4;
        uint64_t count; memcpy(&count, args + off, 8); off += 8;
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        ret = (int32_t)fn(devPtr, value, count, stream);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 108) { /* cudaGetDevice */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGetDevice");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t device_buf[4] = {0};
        ret = (int32_t)fn(device_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), device_buf, device_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 109) { /* cudaDeviceGetStreamPriorityRange */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaDeviceGetStreamPriorityRange");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t leastPriority_buf[4] = {0};
        uint8_t greatestPriority_buf[4] = {0};
        ret = (int32_t)fn(leastPriority_buf, greatestPriority_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), leastPriority_buf, leastPriority_buf + 4);
        response.insert(response.end(), greatestPriority_buf, greatestPriority_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 110) { /* cudaDeviceCanAccessPeer */
      do {
        typedef int (*fn_t)(void*, int, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaDeviceCanAccessPeer");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t canAccessPeer_buf[4] = {0};
        int device; memcpy(&device, args + off, 4); off += 4;
        int peerDevice; memcpy(&peerDevice, args + off, 4); off += 4;
        ret = (int32_t)fn(canAccessPeer_buf, device, peerDevice);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), canAccessPeer_buf, canAccessPeer_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 111) { /* cudaSetDeviceFlags */
      do {
        typedef int (*fn_t)(int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaSetDeviceFlags");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        int flags; memcpy(&flags, args + off, 4); off += 4;
        ret = (int32_t)fn(flags);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 112) { /* cudaGetDeviceCount_v2 */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGetDeviceCount_v2");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t count_buf[4] = {0};
        ret = (int32_t)fn(count_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), count_buf, count_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 113) { /* cudaRuntimeGetVersion */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaRuntimeGetVersion");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t runtimeVersion_buf[4] = {0};
        ret = (int32_t)fn(runtimeVersion_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), runtimeVersion_buf, runtimeVersion_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 114) { /* cudaDriverGetVersion */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaDriverGetVersion");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t driverVersion_buf[4] = {0};
        ret = (int32_t)fn(driverVersion_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), driverVersion_buf, driverVersion_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 115) { /* cudaStreamQuery */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaStreamQuery");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        ret = (int32_t)fn(stream);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 116) { /* cudaStreamWaitEvent */
      do {
        typedef int (*fn_t)(void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaStreamWaitEvent");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        uint64_t event_h; memcpy(&event_h, args + off, 8); off += 8;
        void *event = (void*)(uintptr_t)event_h;
        int flags; memcpy(&flags, args + off, 4); off += 4;
        ret = (int32_t)fn(stream, event, flags);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 117) { /* cudaStreamAddCallback */
      do {
        typedef int (*fn_t)(void*, void*, void*, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaStreamAddCallback");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        uint64_t callback_h; memcpy(&callback_h, args + off, 8); off += 8;
        void *callback = (void*)(uintptr_t)callback_h;
        uint64_t userData_h; memcpy(&userData_h, args + off, 8); off += 8;
        void *userData = (void*)(uintptr_t)userData_h;
        int flags; memcpy(&flags, args + off, 4); off += 4;
        ret = (int32_t)fn(stream, callback, userData, flags);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 118) { /* cudaEventQuery */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaEventQuery");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t event_h; memcpy(&event_h, args + off, 8); off += 8;
        void *event = (void*)(uintptr_t)event_h;
        ret = (int32_t)fn(event);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 119) { /* cudaOccupancyMaxActiveBlocksPerMultiprocessor */
      do {
        typedef int (*fn_t)(void*, void*, int, uint64_t);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaOccupancyMaxActiveBlocksPerMultiprocessor");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t numBlocks_buf[4] = {0};
        uint64_t func_h; memcpy(&func_h, args + off, 8); off += 8;
        void *func = (void*)(uintptr_t)func_h;
        int blockSize; memcpy(&blockSize, args + off, 4); off += 4;
        uint64_t dynamicSMemSize; memcpy(&dynamicSMemSize, args + off, 8); off += 8;
        ret = (int32_t)fn(numBlocks_buf, func, blockSize, dynamicSMemSize);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), numBlocks_buf, numBlocks_buf + 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 120) { /* cudaGetSymbolAddress */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGetSymbolAddress");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *devPtr_real = nullptr;
        uint64_t symbol_h; memcpy(&symbol_h, args + off, 8); off += 8;
        void *symbol = (void*)(uintptr_t)symbol_h;
        ret = (int32_t)fn(&devPtr_real, symbol);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(devPtr_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 121) { /* cudaGetSymbolSize */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGetSymbolSize");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t size_buf[8] = {0};
        uint64_t symbol_h; memcpy(&symbol_h, args + off, 8); off += 8;
        void *symbol = (void*)(uintptr_t)symbol_h;
        ret = (int32_t)fn(size_buf, symbol);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), size_buf, size_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 122) { /* cudaPointerGetAttributes */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaPointerGetAttributes");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t attributes_buf[64] = {0};
        uint64_t ptr_h; memcpy(&ptr_h, args + off, 8); off += 8;
        void *ptr = (void*)(uintptr_t)ptr_h;
        ret = (int32_t)fn(attributes_buf, ptr);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), attributes_buf, attributes_buf + 64);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 123) { /* cudaDeviceEnablePeerAccess */
      do {
        typedef int (*fn_t)(int, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaDeviceEnablePeerAccess");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        int peerDevice; memcpy(&peerDevice, args + off, 4); off += 4;
        int flags; memcpy(&flags, args + off, 4); off += 4;
        ret = (int32_t)fn(peerDevice, flags);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 124) { /* cudaDeviceDisablePeerAccess */
      do {
        typedef int (*fn_t)(int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaDeviceDisablePeerAccess");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        int peerDevice; memcpy(&peerDevice, args + off, 4); off += 4;
        ret = (int32_t)fn(peerDevice);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 125) { /* cudaCreateTextureObject */
      do {
        typedef int (*fn_t)(void*, void*, void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaCreateTextureObject");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint8_t pTexObject_buf[8] = {0};
        uint8_t pResDesc_buf[64]; memcpy(pResDesc_buf, args + off, 64); off += 64;
        uint8_t pTexDesc_buf[64]; memcpy(pTexDesc_buf, args + off, 64); off += 64;
        uint8_t pResViewDesc_buf[64]; memcpy(pResViewDesc_buf, args + off, 64); off += 64;
        ret = (int32_t)fn(pTexObject_buf, pResDesc_buf, pTexDesc_buf, pResViewDesc_buf);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        response.insert(response.end(), pTexObject_buf, pTexObject_buf + 8);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 126) { /* cudaDestroyTextureObject */
      do {
        typedef int (*fn_t)(uint64_t);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaDestroyTextureObject");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t texObject; memcpy(&texObject, args + off, 8); off += 8;
        ret = (int32_t)fn(texObject);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 127) { /* cudaGraphCreate */
      do {
        typedef int (*fn_t)(void*, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGraphCreate");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *pGraph_real = nullptr;
        int flags; memcpy(&flags, args + off, 4); off += 4;
        ret = (int32_t)fn(&pGraph_real, flags);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(pGraph_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 128) { /* cudaGraphDestroy */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGraphDestroy");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t graph_h; memcpy(&graph_h, args + off, 8); off += 8;
        void *graph = lookup_handle(graph_h);
        ret = (int32_t)fn(graph);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 129) { /* cudaGraphInstantiate */
      do {
        typedef int (*fn_t)(void*, void*, uint64_t);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGraphInstantiate");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *pGraphExec_real = nullptr;
        uint64_t graph_h; memcpy(&graph_h, args + off, 8); off += 8;
        void *graph = lookup_handle(graph_h);
        uint64_t flags; memcpy(&flags, args + off, 8); off += 8;
        ret = (int32_t)fn(&pGraphExec_real, graph, flags);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(pGraphExec_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 130) { /* cudaGraphLaunch */
      do {
        typedef int (*fn_t)(void*, void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGraphLaunch");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t graphExec_h; memcpy(&graphExec_h, args + off, 8); off += 8;
        void *graphExec = lookup_handle(graphExec_h);
        uint64_t stream_h; memcpy(&stream_h, args + off, 8); off += 8;
        void *stream = (void*)(uintptr_t)stream_h;
        ret = (int32_t)fn(graphExec, stream);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 131) { /* cudaGraphExecDestroy */
      do {
        typedef int (*fn_t)(void*);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaGraphExecDestroy");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t graphExec_h; memcpy(&graphExec_h, args + off, 8); off += 8;
        void *graphExec = lookup_handle(graphExec_h);
        ret = (int32_t)fn(graphExec);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 132) { /* cudaDeviceGetDefaultMemPool */
      do {
        typedef int (*fn_t)(void*, int);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaDeviceGetDefaultMemPool");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        void *memPool_real = nullptr;
        int device; memcpy(&device, args + off, 4); off += 4;
        ret = (int32_t)fn(&memPool_real, device);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
        { uint64_t h = register_handle(memPool_real);
          response.insert(response.end(), (uint8_t*)&h, (uint8_t*)&h + 8); }
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    if (lib_id == 3 && func_id == 133) { /* cudaMemPoolTrimTo */
      do {
        typedef int (*fn_t)(void*, uint64_t);
        static fn_t fn = (fn_t)dlsym(g_cuda_rt_extra_lib, "cudaMemPoolTrimTo");
        if (!fn) { ret = -1; break; }
        uint32_t off = 0;
        uint64_t memPool_h; memcpy(&memPool_h, args + off, 8); off += 8;
        void *memPool = lookup_handle(memPool_h);
        uint64_t minBytesToKeep; memcpy(&minBytesToKeep, args + off, 8); off += 8;
        ret = (int32_t)fn(memPool, minBytesToKeep);
        response.resize(4);
        memcpy(response.data(), &ret, 4);
      } while(0);
        if (response.empty()) { response.resize(4); memcpy(response.data(), &ret, 4); }
        return true;
    }

    fprintf(stderr, "[WARN] Unknown lib call: lib=%d func=%d\n", lib_id, func_id);
    ret = -1;
    response.resize(4); memcpy(response.data(), &ret, 4);
    return true;
}
